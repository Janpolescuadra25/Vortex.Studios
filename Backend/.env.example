# Vortex Backend — environment template (values are set by JP only)
# Phase 4 will extend this file with: ADMIN_PATH, OWNER_EMAIL,
# OWNER_PASSWORD_HASH, SESSION_SECRET
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
PORT=4000
CORS_ORIGIN=http://localhost:3000
